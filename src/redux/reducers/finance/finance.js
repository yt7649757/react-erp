import * as types from  '../../constants/types';

const initialState = [

]

export const finance = (state = initialState, action) => {
    switch (action.types) {
        default:
            return state
    }
}