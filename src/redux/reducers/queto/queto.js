import * as types from  '../../constants/types';

const initialState = [

]

export const queto = (state = initialState, action) => {
    switch (action.types) {
        default:
            return state
    }
}